apiVersion: v1
kind: Service
metadata:
  name: {{ include "quota-manager.fullname" . }}
  labels:
    {{- include "quota-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "quota-manager.selectorLabels" . | nindent 4 }}
